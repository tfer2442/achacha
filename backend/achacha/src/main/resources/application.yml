spring:
  application:
    name: achacha

  jpa:
    properties:
      hibernate:
        jdbc:
          time_zone=Asia/Seoul:

  profiles:
    group:
      local:
        - local
      prod:
        - prod
      test:
        - test

springdoc:
  swagger-ui:
    path: /api/swagger-ui.html
